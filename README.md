-add directory certs for ssl certificates
-add .env file in base directory containing database info:
PGHOST=localhost
PGPORT=5432
PGDATABASE=database name
PGUSER=database_user
PGPASSWORD=database_password
